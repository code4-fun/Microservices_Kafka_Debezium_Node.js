apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tickets
  template:
    metadata:
      labels:
        app: tickets
    spec:
      initContainers:
        - name: tickets-wait-for-mongo
          image: mongo:6.0
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              echo "Waiting for MongoDB to be ready...";
              until mongosh --host tickets-mongo-0.tickets-mongo-srv:27017 --eval "db.adminCommand('ping')" --quiet; do
                echo "Waiting for MongoDB...";
                sleep 2;
              done;
              echo "MongoDB is ready!";

              echo "Waiting for replica set to be ready...";
              until mongosh --host tickets-mongo-0.tickets-mongo-srv:27017 --eval "
                try {
                  var status = rs.status();
                  if (status.ok && status.ok === 1) {
                    quit(0);
                  }
                } catch (e) {
                  print(\"Error:\", e.codeName);
                  if (e.message.includes('no replset config has been received') || e.codeName === 'NotYetInitialized') {
                    quit(1);
                  }
                  throw e;
                }
              " --quiet; do
                echo "Replica set not ready yet, waiting...";
                sleep 20;
              done;
              echo "Replica set is ready!";
      containers:
        - name: tickets
          image: sdsd123654/tickets
          imagePullPolicy: IfNotPresent
          env:
            - name: KAFKA_CLIENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: KAFKA_URL
              value: 'kafka-srv:9092'
            - name: MONGO_URI
              value: 'mongodb://tickets-mongo-0.tickets-mongo-srv:27017/tickets?replicaSet=rs0'
            - name: JWT_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_KEY
---
apiVersion: v1
kind: Service
metadata:
  name: tickets-srv
spec:
  selector:
    app: tickets
  type: ClusterIP
  ports:
    - name: tickets
      protocol: TCP
      port: 3000
      targetPort: 3000
